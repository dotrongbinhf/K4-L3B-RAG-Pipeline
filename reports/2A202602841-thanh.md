# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Đoàn Quang Thanh
- Mã học viên: 2A202602841
- Nhóm: FourGuys
- Repository/branch: `https://github.com/dotrongbinhf/K4-L3B-RAG-Pipeline` / `main`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 7 — Reranking | Kết hợp các danh sách kết quả bằng Reciprocal Rank Fusion (RRF), loại ID trùng và giữ thứ tự kết quả ổn định. | `src/task7_reranking.py`; `257db18` | Done |
| Task 8 — PageIndex | Tích hợp gửi tài liệu và truy vấn PageIndex; bổ sung retry cho lỗi upload tạm thời. | `src/task8_pageindex_vectorless.py`; `74087ac`, `4b5fabb` | Done |
| Task 9 — Retrieval pipeline | Kết hợp dense, lexical và RRF; dùng PageIndex làm tuyến dự phòng theo ngưỡng điểm. | `src/task9_retrieval_pipeline.py`; `a967f6f` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Dùng RRF để hợp nhất thứ hạng dense và lexical.  
   **Lý do/evidence:** Commit `257db18` triển khai điểm reciprocal-rank, khử trùng theo ID và đầu ra theo schema kết quả chung.  
   **Trade-off:** RRF kết hợp thứ hạng mà không cần hiệu chỉnh trực tiếp thang điểm; chất lượng phụ thuộc vào các danh sách đầu vào và hằng số rank.

2. **Quyết định:** Chuyển sang PageIndex khi retrieval nội bộ không đủ điểm.  
   **Lý do/evidence:** Commit `a967f6f` nối tuyến dense/lexical/fusion với fallback theo ngưỡng; lỗi dịch vụ cần được xử lý riêng.  
   **Trade-off:** Fallback tăng khả năng có nguồn khi dịch vụ khả dụng, nhưng phụ thuộc API/key và độ trễ mạng.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `pytest tests/test_contracts.py -q`; `pytest tests/test_acceptance.py -q`; `pytest -q` (kiểm tra toàn dự án).
- Kết quả trước/sau nếu có: Contracts: 15 passed; acceptance: 5 passed; toàn dự án: 20 passed.
- Lỗi đã phát hiện và cách xử lý: Upload PageIndex có thể thất bại tạm thời do lỗi kết nối; đã bổ sung retry cho lỗi upload transient trong `4b5fabb`.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: PageIndex cần API key và dịch vụ bên ngoài; khi dịch vụ lỗi hoặc chưa có cây tài liệu, tuyến fallback không thể cung cấp kết quả.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Bổ sung đo độ trễ và chất lượng retrieval trên tập câu hỏi lớn hơn, đồng thời theo dõi các lần fallback.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-25
- Tên thành viên: Đoàn Quang Thanh
