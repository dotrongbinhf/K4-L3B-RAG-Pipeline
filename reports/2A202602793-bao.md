# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Đào Gia Bảo
- Mã học viên: 2A202602793
- Nhóm: FourGuys
- Repository/branch: `https://github.com/FourGuys/K4-L3B-RAG-Pipeline` / `main`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 3 — Chuyển đổi PDF pháp lý | Điều chỉnh OCR để xử lý PDF có chữ ký số và cập nhật cách ghi nhận rằng bản gốc được giữ nguyên. | `src/task3_convert_markdown.py`; `10a7bc6` | Done |
| Evaluation/submission artifacts | Đóng góp tích hợp bộ câu hỏi golden và artefact đánh giá vào luồng hoàn thiện bài nộp. | `group_project/evaluation/golden_dataset.json`, `group_project/evaluation/RESULT.md`; `fcd349d` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Chỉ vô hiệu chữ ký trên bản PDF tạm dùng cho OCR.  
   **Lý do/evidence:** Commit `10a7bc6` thêm `--invalidate-digital-signatures` cho OCRmyPDF và ghi rõ PDF gốc trong landing không bị thay đổi.  
   **Trade-off:** Cho phép OCR trang PDF đã ký; kết quả Markdown không thay thế bản gốc có giá trị chữ ký.

2. **Quyết định:** Đưa câu hỏi đánh giá vào golden dataset có thể kiểm tra trong repo.  
   **Lý do/evidence:** Commit `fcd349d` thêm `golden_dataset.json` và kết quả đánh giá để phục vụ kiểm tra submission.  
   **Trade-off:** Tập câu hỏi cố định giúp so sánh lặp lại nhưng phạm vi và độ bao phủ còn giới hạn.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `pytest tests/test_contracts.py -q`; `pytest tests/test_acceptance.py -q`; `pytest -q` (kiểm tra toàn dự án).
- Kết quả trước/sau nếu có: Contracts: 15 passed; acceptance: 5 passed; toàn dự án: 20 passed.
- Lỗi đã phát hiện và cách xử lý: Không có test thất bại; có 18 cảnh báo deprecation từ dependency trong contracts và full suite.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: OCR tiếng Việt có thể sai dấu, bảng hoặc bố cục; golden dataset có quy mô hữu hạn và không đại diện mọi câu hỏi thực tế.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Kiểm tra thủ công các trang OCR khó và mở rộng/đa dạng hóa câu hỏi golden cùng tiêu chí chấm.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-25
- Tên thành viên: Đào Gia Bảo
