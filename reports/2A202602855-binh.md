# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Đỗ Trọng Bình
- Mã học viên: 2A202602855
- Nhóm: FourGuys
- Repository/branch: `https://github.com/FourGuys/K4-L3B-RAG-Pipeline` / `main`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 6 — Lexical search | Cải thiện BM25: lọc chunk theo token giao nhau để không bỏ kết quả phù hợp chỉ vì IDF bằng/nhỏ hơn không; sắp xếp ổn định. | `src/task6_lexical_search.py`; `426ddb0` | Done |
| Tích hợp UI và generation | Mở rộng giao diện chọn chiến lược retrieval, hiển thị nguồn/metrics và bổ sung kiểm tra citation cùng safe refusal. | `app.py`, `src/task10_generation.py`; `0b6a6c3` | Done |
| Tương thích metadata semantic search | Khôi phục trường URL tùy chọn khi vector store bỏ metadata có giá trị null. | `src/task5_semantic_search.py`; `0b6a6c3` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Giữ lexical result khi token query khớp dù BM25 score không dương.  
   **Lý do/evidence:** Commit `426ddb0` lọc theo token overlap và xếp theo score rồi ID thay vì loại toàn bộ score không dương.  
   **Trade-off:** Giảm nguy cơ mất kết quả trong corpus nhỏ; score không dương vẫn cần được hiểu trong ngữ cảnh xếp hạng.

2. **Quyết định:** Chỉ hiển thị câu trả lời có citation hợp lệ và có safe refusal.  
   **Lý do/evidence:** Commit `0b6a6c3` kiểm tra nhãn `[Document N]`, trả lời an toàn khi thiếu citation và gắn nhãn nguồn cho UI.  
   **Trade-off:** Tăng khả năng kiểm chứng; câu trả lời đúng nhưng thiếu định dạng citation cũng có thể bị từ chối.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `pytest tests/test_contracts.py -q`; `pytest tests/test_acceptance.py -q`; `pytest -q` (kiểm tra toàn dự án).
- Kết quả trước/sau nếu có: Contracts: 15 passed; acceptance: 5 passed; toàn dự án: 20 passed.
- Lỗi đã phát hiện và cách xử lý: Không có test thất bại; có 18 cảnh báo deprecation từ dependency trong contracts và full suite.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Chất lượng BM25 phụ thuộc tokenizer và corpus; citation validation kiểm tra chỉ số tài liệu nhưng không chứng minh ngữ nghĩa của từng trích dẫn.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Thêm kiểm thử riêng cho biên của BM25 và citation, rồi đánh giá trải nghiệm UI với truy vấn thực tế.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-25
- Tên thành viên: Đỗ Trọng Bình
